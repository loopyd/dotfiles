for tool_path in "$HOME/.local/share/mise/shims" "$HOME/.cargo/bin" "$HOME/.local/bin"; do
    case ":$PATH:" in
        *":$tool_path:"*) ;;
        *) PATH="$tool_path:$PATH" ;;
    esac
done
export PATH
unset tool_path
