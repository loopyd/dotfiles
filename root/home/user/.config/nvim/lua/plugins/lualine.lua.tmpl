return {
  "nvim-lualine/lualine.nvim",
  name = "nvim-lualine",
  config = function(_, opts)
    require('lualine').setup(opts)
  end,
  opts = {
    options = {
      theme = "wombat",
    },
  },
  @@DOTFILES:SECRET_AGENTS_SKILLS_HINDSIGHT_DOCS_REFERENCES_BEST_PRACTICES_MD_KEY_2@@ = 500,
}
