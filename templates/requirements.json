{
  "python": ">=3.11",
  "herdr": "0.8.2 fresh-install binary, existing binary preserved; scripts/install-herdr.sh",
  "alacritty": "0.18.0-dev source f99dc71708d31d5c32d4b3fa611f9a87bf22657e; scripts/install-alacritty.sh",
  "departure_mono_nerd_font": "3.4.0, verified against the installed regular font",
  "mise_installer": "2026.5.0 (verified fresh-install binary; existing installations preserved)",
  "user_packages": "templates/packages.json is versionless; scripts/install-user-tools.sh restores it after private rendering. Configured runtime defaults remain in the mise/rustup templates.",
  "9router": "0.5.69",
  "easyllama": "0.6.0",
  "hindsight_cli": "0.9.2",
  "hindsight_coding_agents": "0.5.3",
  "required_existing_tools": ["docker", "docker compose", "node >=22.15", "npm", "codex"],
  "hindsight_installer": "scripts/install-hindsight.sh installs CLI 0.9.2 and missing coding-agent runtime >=0.5.3, pulls missing pinned images and enables database/app user units. --dry-run previews; --check is read-only; --no-start defers user service startup.",
  "restore_order": [
    "Install Docker/Compose and Python >=3.11. Node/npm, Codex and 9router can be restored by the opt-in user-tools installer after rendering; EasyLlama remains a separate setup.",
    "Initialize 9router once, then stop it before restoring its exported configuration tables.",
    "Render with a private values file and optionally --restore-router. OAuth/account stores were excluded and require fresh login.",
    "Run scripts/install-user-tools.sh to install mise, configured runtimes and versionless user packages; review local Go rebuilds and excluded retired Pi packages.",
    "Start EasyLlama and 9router-local, then run scripts/install-hindsight.sh as the logged-in destination user to install clients and activate the Hindsight user services.",
    "Start a new Codex session after the local services and coding-agent runtime are available."
  ],
  "hindsight_data": "Only empty persistent-data directories are created. Database contents and the imported corpus require a separate private backup.",
  "hindsight_images": "Pinned image digests and resource limits are preserved in the compose.yaml template."
}
